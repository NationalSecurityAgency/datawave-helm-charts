apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zookeeper
  namespace: default
spec:
  interval: 10m
  chart:
    spec:
      chart: ./zookeeper
      sourceRef:
        kind: GitRepository
        name: datawave-helm-charts
        namespace: flux-system
      interval: 5m
  values: {}
