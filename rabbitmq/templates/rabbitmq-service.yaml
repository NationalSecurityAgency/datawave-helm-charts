####################################
# Core Helm Template Configuration #
####################################

apiVersion: v1
kind: Service

######################################
# Basic Metadata for this Deployment #
######################################

metadata:
  name: "{{ .Values.rabbitmqService.meta.name }}"
  labels:
    application: "{{ .Values.rabbitmqService.meta.name }}"

##################################
# Complete Service Specification #
##################################

spec:

  ###############################
  # Pod Selection Configuration #
  ###############################

  selector:
    application: "{{ .Values.rabbitmqDeployment.meta.name }}"

  ##############################
  # Service Port Configuration #
  ##############################

  ports:

    - name: amqp
      port: {{ .Values.rabbitmqService.ports.amqp }}
      targetPort: 5672

    - name: mgmt
      port: {{ .Values.rabbitmqService.ports.mgmt }}
      targetPort: 15672
