####################################
# Core Helm Template Configuration #
####################################

apiVersion: apps/v1
kind: Deployment

######################################
# Basic Metadata for this Deployment #
######################################

metadata:
  name: "{{ .Values.messagingDeployment.meta.name }}"
  labels:
    application: "{{ .Values.messagingDeployment.meta.name }}"

#####################################
# Complete Deployment Specification #
#####################################

spec:

  ######################################
  # Replication / Update Configuration #
  ######################################

  replicas: {{ .Values.messagingDeployment.replication.replicaCount }}
  revisionHistoryLimit: {{ .Values.messagingDeployment.replication.revisionHistoryLimit }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ .Values.messagingDeployment.replication.maxSurge }}
      maxUnavailable: {{ .Values.messagingDeployment.replication.maxUnavailable }}

  ###################################
  # Resource Selector Configuration #
  ###################################

  selector:
    matchLabels:
      application: "{{ .Values.messagingDeployment.meta.name }}"
  
  #####################################
  # Deployment Template Configuration #
  #####################################

  template:

    ##################################################
    # Basic Metadata for this Deployment's Resources #
    ##################################################

    metadata:
      labels:
        application: "{{ .Values.messagingDeployment.meta.name }}"

    #######################################
    # Deployment Resources Specifications #
    #######################################

    spec:
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.pullSecrets }}
      imagePullSecrets:
      {{- range . }}
      - name: {{ . }}
      {{- end }}
      {{- end }}
      securityContext:
        runAsUser: 1000
      restartPolicy: "{{ .Values.messagingDeployment.restartPolicy }}"
      ##############################################
      # Containers Associated with this Deployment #
      ##############################################

      containers:

        - name: "{{ .Values.messagingDeployment.meta.name }}"
          image: "{{ .Values.messagingDeployment.image.name }}:{{ .Values.messagingDeployment.image.tag }}"
          imagePullPolicy: "{{ .Values.messagingDeployment.image.pullPolicy }}"
          ports:
            - containerPort: 5672
            - containerPort: 15672
          resources: {}
          volumeMounts:
            - name: "{{ .Values.volumes.rabbitmq.name }}"
              mountPath: "{{ .Values.volumes.rabbitmq.destination }}"
              readOnly: true
          {{- if .Values.messagingDeployment.livenessProbe.enabled }}
          livenessProbe:
            exec:
              command:
                - /bin/bash
                - -ec
                - rabbitmq-diagnostics -q check_running
            initialDelaySeconds: {{ .Values.messagingDeployment.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.messagingDeployment.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.messagingDeployment.livenessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.messagingDeployment.livenessProbe.successThreshold }}
            failureThreshold: {{ .Values.messagingDeployment.livenessProbe.failureThreshold }}
          {{- else if .Values.customLivenessProbe }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customLivenessProbe "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.messagingDeployment.readinessProbe.enabled }}
          readinessProbe:
            exec:
              command:
                - /bin/bash
                - -ec
                - rabbitmq-diagnostics -q check_running
            initialDelaySeconds: {{ .Values.messagingDeployment.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.messagingDeployment.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.messagingDeployment.readinessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.messagingDeployment.readinessProbe.successThreshold }}
            failureThreshold: {{ .Values.messagingDeployment.readinessProbe.failureThreshold }}
          {{- else if .Values.customReadinessProbe }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.customReadinessProbe "context" $) | nindent 12 }}
          {{- end }}
      
      ###########################################
      # Volumes Associated with this Deployment #
      ###########################################

      volumes:
        
        - name: "{{ .Values.volumes.rabbitmq.name }}"
          configMap:
            name: "{{ .Values.messagingMap.meta.name }}"
