####################################
# Core Helm Template Configuration #
####################################

apiVersion: v1
kind: Service

######################################
# Basic Metadata for this Deployment #
######################################

metadata:
  name: "{{ .Chart.Name }}-{{ .Values.web.modificationService.meta.name }}"
  labels:
    application: "{{ .Chart.Name }}-{{ .Values.web.modificationService.meta.name }}"

##################################
# Complete Service Specification #
##################################

spec:

  ###############################
  # Pod Selection Configuration #
  ###############################

  selector:
    application: "{{ .Chart.Name }}-{{ .Values.web.modificationDeployment.meta.name }}"

  ##############################
  # Service Port Configuration #
  ##############################

  ports:

    - name: http
      port: {{ .Values.web.modificationService.ports.http }}
      targetPort: 8080

    - name: https
      port: {{ .Values.web.modificationService.ports.https }}
      targetPort: 8443
