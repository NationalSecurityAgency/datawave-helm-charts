####################################
# Core Helm Template Configuration #
####################################

apiVersion: apps/v1
kind: Deployment

######################################
# Basic Metadata for this Deployment #
######################################

metadata:
  name: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqDeployment.meta.name }}"
  labels:
    application: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqDeployment.meta.name }}"

#####################################
# Complete Deployment Specification #
#####################################

spec:

  ######################################
  # Replication / Update Configuration #
  ######################################

  replicas: {{ .Values.web.rabbitmqDeployment.replication.replicaCount }}
  revisionHistoryLimit: {{ .Values.web.rabbitmqDeployment.replication.revisionHistoryLimit }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: {{ .Values.web.rabbitmqDeployment.replication.maxSurge }}
      maxUnavailable: {{ .Values.web.rabbitmqDeployment.replication.maxUnavailable }}

  ###################################
  # Resource Selector Configuration #
  ###################################

  selector:
    matchLabels:
      application: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqDeployment.meta.name }}"
  
  #####################################
  # Deployment Template Configuration #
  #####################################

  template:

    ##################################################
    # Basic Metadata for this Deployment's Resources #
    ##################################################

    metadata:
      labels:
        application: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqDeployment.meta.name }}"

    #######################################
    # Deployment Resources Specifications #
    #######################################

    spec:
      {{- with .Values.web.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.web.pullSecrets }}
      imagePullSecrets:
      {{- range . }}
      - name: {{ . }}
      {{- end }}
      {{- end }}
      securityContext:
        runAsUser: 1000
      restartPolicy: "{{ .Values.web.rabbitmqDeployment.restartPolicy }}"
      ##############################################
      # Containers Associated with this Deployment #
      ##############################################

      containers:

        - name: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqDeployment.meta.name }}"
          image: "{{ .Values.web.rabbitmqDeployment.image.name }}:{{ .Values.web.rabbitmqDeployment.image.tag }}"
          imagePullPolicy: "{{ .Values.web.rabbitmqDeployment.image.pullPolicy }}"
          ports:
            - containerPort: 5672
            - containerPort: 15672
          resources: {}
          volumeMounts:
            - name: "{{ .Chart.Name }}-{{ .Values.web.volumes.rabbitmq.name }}"
              mountPath: "{{ .Values.web.volumes.rabbitmq.destination }}"
              readOnly: true
          {{- if .Values.web.rabbitmqDeployment.livenessProbe.enabled }}
          livenessProbe:
            exec:
              command:
                - /bin/bash
                - -ec
                - rabbitmq-diagnostics -q check_running
            initialDelaySeconds: {{ .Values.web.rabbitmqDeployment.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.web.rabbitmqDeployment.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.web.rabbitmqDeployment.livenessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.web.rabbitmqDeployment.livenessProbe.successThreshold }}
            failureThreshold: {{ .Values.web.rabbitmqDeployment.livenessProbe.failureThreshold }}
          {{- else if .Values.web.customLivenessProbe }}
          livenessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.web.customLivenessProbe "context" $) | nindent 12 }}
          {{- end }}
          {{- if .Values.web.rabbitmqDeployment.readinessProbe.enabled }}
          readinessProbe:
            exec:
              command:
                - /bin/bash
                - -ec
                - rabbitmq-diagnostics -q check_running
            initialDelaySeconds: {{ .Values.web.rabbitmqDeployment.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.web.rabbitmqDeployment.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.web.rabbitmqDeployment.readinessProbe.timeoutSeconds }}
            successThreshold: {{ .Values.web.rabbitmqDeployment.readinessProbe.successThreshold }}
            failureThreshold: {{ .Values.web.rabbitmqDeployment.readinessProbe.failureThreshold }}
          {{- else if .Values.web.customReadinessProbe }}
          readinessProbe: {{- include "common.tplvalues.render" (dict "value" .Values.web.customReadinessProbe "context" $) | nindent 12 }}
          {{- end }}
      
      ###########################################
      # Volumes Associated with this Deployment #
      ###########################################

      volumes:
        - name: "{{ .Chart.Name }}-{{ .Values.web.volumes.rabbitmq.name }}"
          configMap:
            name: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqMap.meta.name }}"
