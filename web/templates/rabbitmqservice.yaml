####################################
# Core Helm Template Configuration #
####################################

apiVersion: v1
kind: Service

######################################
# Basic Metadata for this Deployment #
######################################

metadata:
  name: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqService.meta.name }}"
  labels:
    application: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqService.meta.name }}"

##################################
# Complete Service Specification #
##################################

spec:

  ###############################
  # Pod Selection Configuration #
  ###############################

  selector:
    application: "{{ .Chart.Name }}-{{ .Values.web.rabbitmqDeployment.meta.name }}"

  ##############################
  # Service Port Configuration #
  ##############################

  ports:

    - name: amqp
      port: {{ .Values.web.rabbitmqService.ports.amqp }}
      targetPort: 5672

    - name: mgmt
      port: {{ .Values.web.rabbitmqService.ports.mgmt }}
      targetPort: 15672
